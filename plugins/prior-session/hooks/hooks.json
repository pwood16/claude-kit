{
  "description": "Capture session summaries on clear or exit",
  "hooks": {
    "SessionEnd": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks-handlers/capture-session.sh"
          }
        ]
      }
    ]
  }
}
